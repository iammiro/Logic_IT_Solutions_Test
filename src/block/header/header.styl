.header
  flex-box(flex, row, space-around, center)
  font-size 30px
  font-family 'Raleway', sans-serif
  font-weight 700
  color #ffffff
  text-transform uppercase
  line-height 0.8
  text-align center

@media (max-width: 990px)
  .header
    padding 0 55px
    line-height 1.5