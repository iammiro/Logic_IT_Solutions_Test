.slider
  width 100%
.slider__slide
  flex-box(flex, column, flex-start, center)
  background  url("../img/BG.png") 100% 100% no-repeat
  overflow hidden
  height 500px
  background-size cover
  background-position center
.header-navigation
  flex-box(flex, row, space-between, center)